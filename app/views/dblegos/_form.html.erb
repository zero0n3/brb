<%= form_with(model: dblego, local: true) do |form| %>
  <% if dblego.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dblego.errors.count, "error") %> prohibited this dblego from being saved:</h2>

      <ul>
      <% dblego.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :part %>
    <%= form.text_field :part, id: :dblego_part %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.number_field :color, id: :dblego_color %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :dblego_quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
